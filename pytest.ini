[pytest]
DJANGO_SETTINGS_MODULE = praman.settings
python_files = tests.py test_*.py *_tests.py
addopts = -ra --strict-markers
markers =
    slow: tests that hit the disk / spawn Docling (skipped with -m "not slow")
    network: tests that call external APIs (skipped by default)
