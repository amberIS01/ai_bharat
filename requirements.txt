# Praman — Python dependencies (Day 1)
# Install: python3 -m venv .venv && source .venv/bin/activate && pip install -r requirements.txt

# --- Django stack ---
Django>=5.0,<6.0
django-htmx>=1.17
python-decouple>=3.8

# --- Document parsing ---
docling>=2.0
pdf2image>=1.17
PyMuPDF>=1.24
# (Removed paddleocr / paddlepaddle on 2026-05-02: paddlepaddle 3.3.1 OneDNN
# regression broke inference and we pivoted to Gemini Flash Vision for OCR.)

# --- LLM API ---
google-genai>=1.0
pydantic>=2.0
httpx>=0.27

# --- Synthetic data generation ---
weasyprint>=62.0
Jinja2>=3.1
augraphy>=8.2

# --- PDF signing / audit ---
pyhanko>=0.25
cryptography>=42.0

# --- Dev / test ---
pytest>=8.0
pytest-django>=4.8
ruff>=0.4
